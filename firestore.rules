rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to all documents
    match /{document=**} {
      allow read: if true;
    }
    // Allow public write access to the analytics collection for visitor stats
    match /analytics/{document} {
      allow write: if true;
    }
  }
}
